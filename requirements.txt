# PR Review Bot — Python dependencies
# Install with: pip install -r requirements.txt

# GitHub API client — used to fetch PR diffs and post comments
PyGithub==2.3.0

# OpenAI-compatible client — used for Groq API calls
openai==1.30.5

# Official Google AI library — used for Gemini (more stable)
google-generativeai==0.8.3

# Loads variables from .env file during local development
# Has no effect in GitHub Actions (env vars are injected by the workflow)
python-dotenv==1.0.1
